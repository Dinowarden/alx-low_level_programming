#include "main.h"
/**
 * _islower -> checks if the character is lowercase
 * @c: a charcter argument
 * Return: returns 1 and 0 depending on condition
 * print_alphabet -> prints the lowercase alphabets                         * main - check the code                                                    * Return: Always 0.                                                        */     
int _islower(int c)

{

		return (c >= 'a' && c <= 'z');

}
